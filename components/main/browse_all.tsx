import React from 'react'

type Props = {}

const BrowseAll = (props: Props) => {
  const categories = ['podcast','madeforyou', 'charts', 'newreleases', 'discover', 'concerts'];
  return (
    <div>BrowseAll</div>
  )
}

export default BrowseAll