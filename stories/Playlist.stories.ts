import type { Meta, StoryObj } from '@storybook/react';
import Playlist from '../components/main/playlist';

const meta = {
  title: 'SpotifyUI/Playlist',
  component: Playlist,

};


export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
};